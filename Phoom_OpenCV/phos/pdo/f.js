function F($a) // 5gl to php bootstrap code, use function argument as input string; add colon definition;
{   // $S[]=":END:";

//    global $argv, $S, $SS, $xk, $xs, $SC, $SL, $CDW; // $CDW: colon defined words

alert($a);

}
